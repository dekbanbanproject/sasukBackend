
$(window).on("load", function(){

    const progresst_per = document.querySelector('.progress-per');
    progresst_per.style.width = progresst_per.getAttribute('data-per') + '%';
    progresst_per.style.opacity = 1;

    const progresst_assets = document.querySelector('.progress-assets');
    progresst_assets.style.width = progresst_assets.getAttribute('data-assets') + '%';
    progresst_assets.style.opacity = 1;

    const progresst_assetbuildings = document.querySelector('.progress-assetbuildings');
    progresst_assetbuildings.style.width = progresst_assetbuildings.getAttribute('data-assetbuildings') + '%';
    progresst_assetbuildings.style.opacity = 1;

    const progresst_hospital = document.querySelector('.progress-hospital');
    progresst_hospital.style.width = progresst_hospital.getAttribute('data-hospital') + '%';
    progresst_hospital.style.opacity = 1;







    





    const progresst_chaingmai = document.querySelector('.progress-chaingmai');
    progresst_chaingmai.style.width = progresst_chaingmai.getAttribute('data-chaingmai') + '%';
    progresst_chaingmai.style.opacity = 1;

    const progresst_lampangss = document.querySelector('.progress-lampangss');
    progresst_lampangss.style.width = progresst_lampangss.getAttribute('data-lampangss') + '%';
    progresst_lampangss.style.opacity = 1;

    const progresst_nans = document.querySelector('.progress-nans');
    progresst_nans.style.width = progresst_nans.getAttribute('data-nans') + '%';
    progresst_nans.style.opacity = 1;


    const progresst_chaingrayss = document.querySelector('.progress-chaingrayss');
    progresst_chaingrayss.style.width = progresst_chaingrayss.getAttribute('data-chaingrayss') + '%';
    progresst_chaingrayss.style.opacity = 1;








  

    const progresst_bc = document.querySelector('.progress-bc');
    progresst_bc.style.width = progresst_bc.getAttribute('data-bc') + '%';
    progresst_bc.style.opacity = 1;

    const progresst_lp = document.querySelector('.progress-lp');
    progresst_lp.style.width = progresst_lp.getAttribute('data-lp') + '%';
    progresst_lp.style.opacity = 1;

    const progresst_lap = document.querySelector('.progress-lap');
    progresst_lap.style.width = progresst_lap.getAttribute('data-lap') + '%';
    progresst_lap.style.opacity = 1;
    
    const progresst_laprae = document.querySelector('.progress-laprae');
    progresst_laprae.style.width = progresst_laprae.getAttribute('data-laprae') + '%';
    progresst_laprae.style.opacity = 1;

    const progresst_nan = document.querySelector('.progress-nan');
    progresst_nan.style.width = progresst_nan.getAttribute('data-nan') + '%';
    progresst_nan.style.opacity = 1;

    const progresst_payoa = document.querySelector('.progress-payoa');
    progresst_payoa.style.width = progresst_payoa.getAttribute('data-payoa') + '%';
    progresst_payoa.style.opacity = 1;

    const progresst_chaingray = document.querySelector('.progress-chaingray');
    progresst_chaingray.style.width = progresst_chaingray.getAttribute('data-chaingray') + '%';
    progresst_chaingray.style.opacity = 1;

    const progresst_maehongson = document.querySelector('.progress-maehongson');
    progresst_maehongson.style.width = progresst_maehongson.getAttribute('data-maehongson') + '%';
    progresst_maehongson.style.opacity = 1;
   
});